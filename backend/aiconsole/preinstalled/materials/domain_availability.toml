name = "Domain Availability"
version = "0.0.2"
usage = "When you need to check if a domain is available or not"
usage_examples = ["Check if 'github.com' and 'google.com' are available?"]
default_status = "enabled"
content_type = "api"
content = "file://./domain_availability.py"
